$("#msg-list").html("");
<% @messages.reverse_each do |message| %>
$("#msg-list").append("<%= j render(:partial => 'show_message', :locals => { :user => @user, :friend => @friend, :m => message }) %>");
<% end %>

